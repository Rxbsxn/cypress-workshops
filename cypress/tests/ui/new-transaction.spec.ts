describe("New Transaction", function () {

  beforeEach(function () {
    cy.task("db:seed");
  });

  it("navigates to the new transaction form, selects a user and submits a transaction payment", function () {
  });

  it("navigates to the new transaction form, selects a user and submits a transaction request", function () {
    
  });

  it("displays new transaction errors", function () {
    
  });

  xit("submits a transaction payment and verifies the deposit for the receiver", function () {
  
  });

  xit("submits a transaction request and accepts the request for the receiver", function () {
    
  });
});
